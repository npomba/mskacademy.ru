import { TypeGeneralColor } from '@/types/index'
import { colors } from '@/config/index'

type TypeThemeColor = TypeGeneralColor

const themeColor: TypeThemeColor = colors.alpha as TypeThemeColor

export default themeColor
